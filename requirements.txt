# requirements.txt

# Core FastAPI and Aiogram
fastapi
uvicorn
uvicorn[standard]
aiogram
python-dotenv

# Database and Asynchronous Handling
sqlalchemy[asyncio]
asyncpg # For PostgreSQL support
psycopg2-binary # Optional: If you need sync PostgreSQL tools (though asyncpg is the primary)

# Algolia Integration
algoliasearch

# Utility for graceful shutdown and multiprocessing
# concurrent.futures is standard, but keeping dependencies clear.

# Note: thefuzz is no longer needed since Algolia handles fuzzy search, saving significant CPU.
