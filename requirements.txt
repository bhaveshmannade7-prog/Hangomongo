# requirements.txt
# --- Core Web Frameworks ---
fastapi
uvicorn[standard] # Includes standard dependencies for high performance
python-dotenv     # For reading .env file variables

# --- Telegram Bot Library ---
aiogram           # Main Telegram bot framework

# --- Database Integration (Async SQLAlchemy with PostgreSQL) ---
sqlalchemy[asyncio] # SQLAlchemy Async Support
asyncpg           # Recommended fast PostgreSQL driver for Asyncio
# psycopg2-binary   # (Optional, removed to keep dependencies lean, use asyncpg)

# --- Algolia Search Integration ---
algoliasearch     # Client for the Algolia service

# --- Utilities ---
# contextlib (standard library)
# typing (standard library)
# functools (standard library)
# concurrent.futures (standard library)
